<Window x:Class="SettingsApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="450" Width="400"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="14">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Settings" FontSize="24" FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Enable notifications" VerticalAlignment="Center" Width="200"/>
            <CheckBox x:Name="CbNotifications" IsChecked="True"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Dark theme" VerticalAlignment="Center" Width="200"/>
            <CheckBox x:Name="CbDarkTheme" IsChecked="False"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Language" VerticalAlignment="Center" Width="200"/>
        </StackPanel>

        <ComboBox Grid.Row="4" x:Name="CbLanguage" Margin="0,0,0,24"
                  SelectedIndex="0">
            <ComboBoxItem Content="English"/>
            <ComboBoxItem Content="简体中文"/>
            <ComboBoxItem Content="日本語"/>
        </ComboBox>

        <TextBlock Grid.Row="5"/>

        <Button Grid.Row="6" Content="Save" Height="36"
                HorizontalAlignment="Stretch"
                Background="#0078D7" Foreground="White"
                BorderThickness="0" FontWeight="Medium"/>
    </Grid>
</Window>
